version: "3"
services:
  sonarqube:
    image: sonarqube:lts
    ports:
      - 9000:9000
    networks:
      - mynetwork
  jenkins:
    build:
      context: .
    ports:
      - 9002:9002
    networks:
      - mynetwork
networks:
  mynetwork:
